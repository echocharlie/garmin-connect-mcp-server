# Optional: override where Garmin OAuth tokens are stored (default: ~/.garminconnect)
# GARMINTOKENS=~/.garminconnect
#
# No other secrets needed. Your Garmin password is entered interactively by login.py
# and is never written to disk.
